import { Colors } from '@/constants/Colors';
import { View } from 'react-native';

const Divider = () => {
  return <View style={{ height: 1, backgroundColor: Colors.default }} />;
};

export default Divider;