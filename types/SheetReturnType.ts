import {
  CategoryType,
  GroupType,
  AmountTypeType,
  CurrencyType,
} from "@/types";

type SheetReturnType = CategoryType | GroupType | AmountTypeType | CurrencyType;

export default SheetReturnType;